  set $locale "en-us";
  if ($http_host="dev-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="dev-ui-blue-alb.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host = "dev-ui-jp.aws.broadcom.com") {
    set $locale "ja-jp";
    set $bclang "ja-jp";
  }
  if ($http_host = "dev-ui-cn.aws.broadcom.com") {
    #set $locale "zh-chs";
    set $locale "zh-cn";
    #set $bclang "zh-chs";
    set $bclang "zh-cn";
  }
  if ($http_host = "qa-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host = "qa-ui-jp.aws.broadcom.com") {
    set $locale "ja-jp";
    set $bclang "ja-jp";
  }
  if ($http_host = "qa-ui-cn.aws.broadcom.com") {
    set $locale "zh-cn";
    #set $locale "zh-chs";
    #set $bclang "zh-chs";
    set $bclang "zh-cn";
  }
  if ($http_host = "stg-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host = "stg-ui-jp.aws.broadcom.com") {
    set $locale "ja-jp";
    set $bclang "ja-jp";
  }
  if ($http_host = "stg-ui-cn.aws.broadcom.com") {
    #set $locale "zh-chs";
    set $locale "zh-cn";
    #set $bclang "zh-chs";
    set $bclang "zh-cn";
  }
  if ($http_host = "prd-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host = "prd-ui-jp.aws.broadcom.com") {
    set $locale "ja-jp";
    set $bclang "ja-jp";
  }
  if ($http_host = "prd-ui-cn.aws.broadcom.com") {
    set $locale "zh-cn";
    #set $locale "zh-chs";
    #set $bclang "zh-chs";
    set $bclang "zh-cn";
  }
  if ($http_host = "dr-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host = "dr-ui-jp.aws.broadcom.com") {
    set $locale "ja-jp";
    set $bclang "ja-jp";
  }
  if ($http_host = "dr-ui-cn.aws.broadcom.com") {
    set $locale "zh-cn";
    #set $locale "zh-chs";
    #set $bclang "zh-chs";
    set $bclang "zh-cn";
  }
  if ($http_host = "cmsstaging.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host = "cmsstagingjp.broadcom.com") {
    set $locale "ja-jp";
    set $bclang "ja-jp";
  }
  if ($http_host = "cmsstaging.broadcom.cn") {
    set $locale "zh-cn";
    #set $locale "zh-chs";
    #set $bclang "zh-chs";
    set $bclang "zh-cn";
  }
  if ($http_host = "cmsgolive.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host = "cmsgolivejp.broadcom.com") {
    set $locale "ja-jp";
    set $bclang "ja-jp";
  }
  if ($http_host = "cmsgolive.broadcom.cn") {
    set $locale "zh-cn";
    #set $locale "zh-chs";
    #set $bclang "zh-chs";
    set $bclang "zh-cn";
  }
  if ($http_host = "www.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host = "jp.broadcom.com") {
    set $locale "ja-jp";
    set $bclang "ja-jp";
  }
  if ($http_host = "www.broadcom.cn") {
    set $locale "zh-cn";
    #set $locale "zh-chs";
    #set $bclang "zh-chs";
    set $bclang "zh-cn";
  }
  if ($http_host="vmdev-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="vmqa-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="vmstg-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="vmprd-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="vmdr-ui.aws.broadcom.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="vmdev-ui.cloud2.vmware.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="vmqa-ui.cloud2.vmware.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="vmstg-ui.cloud2.vmware.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host = "cmsstaging.vmware.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="vmprd-ui.cloud2.vmware.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  if ($http_host="vmdr-ui.cloud2.vmware.com") {
    set $locale "en-us";
    set $bclang "en-us";
  }
  #auth_basic "Restricted Content";
  #auth_basic_user_file /etc/nginx/.htpasswd;

  add_header bc_locale $locale;
  proxy_set_header locale $locale;
  add_header bclang $bclang;
  proxy_set_header bc_lang $bclang;
  rewrite ^/ichabod-test/(.*) /broadcom-faceted-search?http_host=$http_host&host=$host&server_name=$server_name&locale=$locale permanent;

  proxy_set_header Host $http_host;

  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_redirect off;
  proxy_buffering off;
  proxy_pass http://nodeapp;
  proxy_cookie_path ~^/.*$ /;
